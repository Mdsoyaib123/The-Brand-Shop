import { useLoaderData } from "react-router-dom";

const Update = () => {
    const loader = useLoaderData();
    console.log(loader);
    return (
        <div>
            
        </div>
    );
};

export default Update;